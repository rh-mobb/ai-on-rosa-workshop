---
- name: Create IAM roles for Bedrock
  hosts: localhost
  connection: local
  gather_facts: no
  collections:
    - amazon.aws
  vars_files:
    - vars.yml
  roles:
    - fetch_oidc
    - iam_roles
    - create_serviceaccount
    - create_configmap
